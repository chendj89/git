/**
 * 使用弹窗
 * @param {*} file 单文件组件
 * @param {*} options 个性参数
 * @param options.$el 指定接受弹窗容器
 * @tutorial
 * ```js
 * //主动销毁弹窗
 * this.$close(someData);
 * ```
 * @returns
 */
export declare function useDialog(file: any, options?: any): Promise<unknown>;
